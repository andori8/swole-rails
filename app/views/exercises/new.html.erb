<%= render partial: 'application/errors', locals: {object: @exercise} %>

<br>
<h1>Create New Exercise</h1>
<%= form_for @exercise do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <%= f.submit "Create" %>
<% end %>
