<%= render partial: 'application/alerts' %>

<h1>All Workouts</h1>
  <ul>
    <% @workouts.each do |workout| %>
      <li><%= link_to workout.title, workout %></li><br>  
    <% end %>
  </ul>

<%= link_to "Create a New Workout", new_workout_path %>
