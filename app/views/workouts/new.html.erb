<%= render partial: 'application/errors', locals: {object: @workout} %>

<%= form_for @workout do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <%= f.submit "Create" %>
<% end %>

<%= link_to "Go Back", workouts_path %>
